# 🚀 Быстрый старт

## Требования

- Node.js 18+
- npm или yarn
- Discord аккаунт разработчика

## Установка

### 1. Клонирование

```bash
git clone <repo-url>
cd helloworld
npm install
```

### 2. Настройка окружения

Создайте файл `.env`:

```env
BOT1_TOKEN=your_moderation_bot_token
BOT2_TOKEN=your_entertainment_bot_token
BOT3_TOKEN=your_economy_bot_token
BOT4_TOKEN=your_utilities_bot_token

CRYPTO_BOT_TOKEN=your_cryptobot_token  # Для донатов
```

### 3. База данных

```bash
# Создать/обновить схему
npx prisma db push

# Сгенерировать клиент
npx prisma generate

# Заполнить предметы
npm run build
node dist/scripts/seedItems.js
```

### 4. Запуск

**Разработка:**
```bash
npm run dev
```

**Продакшен:**
```bash
npm run build
pm2 start ecosystem.config.cjs
```

## Создание бота в Discord

1. Перейдите на [Discord Developer Portal](https://discord.com/developers/applications)
2. Нажмите "New Application"
3. Во вкладке "Bot" нажмите "Add Bot"
4. Скопируйте токен
5. Включите необходимые Intents:
   - **Presence Intent** — для статусов
   - **Server Members Intent** — для модерации
   - **Message Content Intent** — для сообщений

## Приглашение бота

Сгенерируйте ссылку во вкладке OAuth2 → URL Generator:
- Scopes: `bot`, `applications.commands`
- Permissions: По необходимости

## Проверка

После запуска боты должны:
1. Появиться онлайн в Discord
2. Зарегистрировать слэш-команды (до 1 часа глобально, мгновенно для гильдии)
3. Отвечать на команды

## Устранение неполадок

**Бот не отвечает:**
- Проверьте токен в `.env`
- Проверьте Intents в Developer Portal
- Посмотрите логи: `pm2 logs`

**Команды не регистрируются:**
- Подождите до 1 часа
- Для тестирования используйте гильдейские команды

**Ошибки Prisma:**
- Убедитесь что БД создана: `npx prisma db push`
- Проверьте путь к БД в `prisma/schema.prisma`

